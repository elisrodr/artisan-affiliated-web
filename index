<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Artisan Affiliated — The Future of Food Commerce</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=DM+Mono:wght@300;400;500&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --black: #080808;
      --surface: #0f0f0f;
      --surface2: #161616;
      --border: rgba(255,255,255,0.07);
      --border-bright: rgba(255,255,255,0.14);
      --white: #f5f3ef;
      --white-dim: rgba(245,243,239,0.55);
      --white-dimmer: rgba(245,243,239,0.3);
      --gold: #c9a96e;
      --gold-dim: rgba(201,169,110,0.2);
      --gold-glow: rgba(201,169,110,0.08);
      --green: #4ade80;
      --green-dim: rgba(74,222,128,0.15);
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--black);
      color: var(--white);
      font-family: 'Outfit', sans-serif;
      font-weight: 300;
      overflow-x: hidden;
      cursor: none;
    }

    /* Custom cursor */
    .cursor {
      position: fixed;
      width: 8px; height: 8px;
      background: var(--gold);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
      transition: transform 0.1s ease, width 0.3s ease, height 0.3s ease, background 0.3s ease;
      mix-blend-mode: normal;
    }
    .cursor-ring {
      position: fixed;
      width: 36px; height: 36px;
      border: 1px solid rgba(201,169,110,0.4);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9998;
      transform: translate(-50%, -50%);
      transition: transform 0.15s ease, width 0.3s ease, height 0.3s ease, opacity 0.3s ease;
    }
    body:hover .cursor { opacity: 1; }

    /* Noise overlay */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none;
      z-index: 1000;
      opacity: 0.4;
    }

    /* Nav */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 24px 48px;
      background: linear-gradient(to bottom, rgba(8,8,8,0.95) 0%, transparent 100%);
      backdrop-filter: blur(0px);
    }

    .nav-logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 18px;
      font-weight: 300;
      letter-spacing: 0.12em;
      color: var(--white);
      text-decoration: none;
      text-transform: uppercase;
    }
    .nav-logo span {
      color: var(--gold);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 40px;
      list-style: none;
    }
    .nav-links a {
      font-family: 'DM Mono', monospace;
      font-size: 11px;
      font-weight: 400;
      letter-spacing: 0.1em;
      color: var(--white-dim);
      text-decoration: none;
      text-transform: uppercase;
      transition: color 0.2s ease;
    }
    .nav-links a:hover { color: var(--white); }

    .nav-cta {
      font-family: 'DM Mono', monospace;
      font-size: 11px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--gold) !important;
      border: 1px solid rgba(201,169,110,0.4);
      padding: 10px 20px;
      border-radius: 2px;
      transition: background 0.2s ease, border-color 0.2s ease !important;
    }
    .nav-cta:hover {
      background: var(--gold-dim) !important;
      border-color: var(--gold) !important;
      color: var(--gold) !important;
    }

    /* Hero */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      padding: 120px 48px 80px;
      position: relative;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background:
        radial-gradient(ellipse 60% 50% at 70% 50%, rgba(201,169,110,0.06) 0%, transparent 70%),
        radial-gradient(ellipse 40% 60% at 20% 80%, rgba(74,222,128,0.03) 0%, transparent 60%),
        linear-gradient(135deg, #080808 0%, #0d0d0d 100%);
    }

    .hero-grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
      background-size: 80px 80px;
      mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 0%, transparent 100%);
    }

    .hero-eyebrow {
      font-family: 'DM Mono', monospace;
      font-size: 11px;
      font-weight: 400;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 32px;
      display: flex;
      align-items: center;
      gap: 16px;
      position: relative;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.2s forwards;
    }
    .hero-eyebrow::before {
      content: '';
      width: 40px;
      height: 1px;
      background: var(--gold);
      opacity: 0.6;
    }

    .hero-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(52px, 8vw, 110px);
      font-weight: 300;
      line-height: 0.92;
      letter-spacing: -0.02em;
      color: var(--white);
      max-width: 900px;
      position: relative;
      margin-bottom: 40px;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.4s forwards;
    }
    .hero-title em {
      font-style: italic;
      color: var(--gold);
    }

    .hero-sub {
      font-family: 'Outfit', sans-serif;
      font-size: 18px;
      font-weight: 300;
      line-height: 1.7;
      color: var(--white-dim);
      max-width: 520px;
      position: relative;
      margin-bottom: 56px;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.6s forwards;
    }

    .hero-actions {
      display: flex;
      align-items: center;
      gap: 24px;
      position: relative;
      opacity: 0;
      animation: fadeUp 0.8s ease 0.8s forwards;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: var(--gold);
      color: var(--black);
      font-family: 'DM Mono', monospace;
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      padding: 16px 32px;
      border-radius: 2px;
      border: none;
      cursor: none;
      transition: background 0.2s ease, transform 0.2s ease;
    }
    .btn-primary:hover {
      background: #d4b47e;
      transform: translateY(-1px);
    }

    .btn-secondary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: transparent;
      color: var(--white-dim);
      font-family: 'DM Mono', monospace;
      font-size: 12px;
      font-weight: 400;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      text-decoration: none;
      padding: 16px 0;
      border: none;
      cursor: none;
      transition: color 0.2s ease;
    }
    .btn-secondary:hover { color: var(--white); }
    .btn-secondary .arrow { transition: transform 0.2s ease; }
    .btn-secondary:hover .arrow { transform: translateX(4px); }

    .hero-stat-row {
      position: absolute;
      bottom: 60px;
      left: 48px;
      right: 48px;
      display: flex;
      align-items: center;
      gap: 0;
      border-top: 1px solid var(--border);
      padding-top: 40px;
      opacity: 0;
      animation: fadeUp 0.8s ease 1s forwards;
    }

    .hero-stat {
      flex: 1;
      padding-right: 40px;
    }
    .hero-stat + .hero-stat {
      padding-left: 40px;
      border-left: 1px solid var(--border);
    }
    .hero-stat-number {
      font-family: 'Cormorant Garamond', serif;
      font-size: 36px;
      font-weight: 300;
      color: var(--white);
      line-height: 1;
      margin-bottom: 6px;
    }
    .hero-stat-label {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--white-dimmer);
    }

    /* Section base */
    section {
      padding: 120px 48px;
      position: relative;
    }

    .section-label {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 24px;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .section-label::after {
      content: '';
      flex: 0 0 32px;
      height: 1px;
      background: var(--gold);
      opacity: 0.4;
    }

    .section-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(36px, 5vw, 64px);
      font-weight: 300;
      line-height: 1.1;
      letter-spacing: -0.01em;
      color: var(--white);
      margin-bottom: 24px;
    }
    .section-title em {
      font-style: italic;
      color: var(--gold);
    }

    .section-body {
      font-size: 17px;
      font-weight: 300;
      line-height: 1.75;
      color: var(--white-dim);
      max-width: 560px;
    }

    /* KAE Section */
    .kae-section {
      background: var(--surface);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
    }

    .kae-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 80px;
      align-items: start;
    }

    .kae-left { }

    .kae-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--green-dim);
      border: 1px solid rgba(74,222,128,0.2);
      border-radius: 100px;
      padding: 6px 14px;
      margin-bottom: 32px;
    }
    .kae-badge-dot {
      width: 6px;
      height: 6px;
      background: var(--green);
      border-radius: 50%;
      animation: pulse 2s ease infinite;
    }
    .kae-badge-text {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--green);
    }

    .kae-right {
      padding-top: 8px;
    }

    .feature-list {
      display: flex;
      flex-direction: column;
      gap: 0;
      margin-top: 16px;
    }

    .feature-item {
      display: flex;
      align-items: flex-start;
      gap: 20px;
      padding: 28px 0;
      border-bottom: 1px solid var(--border);
      transition: background 0.2s ease;
    }
    .feature-item:first-child { border-top: 1px solid var(--border); }

    .feature-num {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      color: var(--gold);
      opacity: 0.6;
      padding-top: 4px;
      flex-shrink: 0;
      width: 24px;
    }

    .feature-content {}
    .feature-title {
      font-family: 'Outfit', sans-serif;
      font-size: 15px;
      font-weight: 500;
      color: var(--white);
      margin-bottom: 6px;
      letter-spacing: 0.01em;
    }
    .feature-desc {
      font-family: 'Outfit', sans-serif;
      font-size: 14px;
      font-weight: 300;
      color: var(--white-dimmer);
      line-height: 1.6;
    }

    /* Flow diagram */
    .flow-section {
      background: var(--black);
    }

    .flow-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1px;
      background: var(--border);
      margin-top: 64px;
      border: 1px solid var(--border);
    }

    .flow-step {
      background: var(--black);
      padding: 40px 32px;
      position: relative;
      transition: background 0.3s ease;
    }
    .flow-step:hover { background: var(--surface); }

    .flow-step-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 48px;
      font-weight: 300;
      color: var(--border-bright);
      line-height: 1;
      margin-bottom: 24px;
    }
    .flow-step-title {
      font-family: 'Outfit', sans-serif;
      font-size: 15px;
      font-weight: 500;
      color: var(--white);
      margin-bottom: 10px;
    }
    .flow-step-desc {
      font-family: 'Outfit', sans-serif;
      font-size: 13px;
      font-weight: 300;
      color: var(--white-dimmer);
      line-height: 1.65;
    }
    .flow-step-accent {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--gold), transparent);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s ease;
    }
    .flow-step:hover .flow-step-accent { transform: scaleX(1); }

    /* About */
    .about-section {
      background: var(--surface);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
    }

    .about-inner {
      display: grid;
      grid-template-columns: 1fr 1.2fr;
      gap: 100px;
      align-items: center;
    }

    .about-quote {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(28px, 3.5vw, 44px);
      font-weight: 300;
      font-style: italic;
      line-height: 1.3;
      color: var(--white);
      border-left: 2px solid var(--gold);
      padding-left: 32px;
      margin-top: 40px;
    }

    .about-details {
      display: flex;
      flex-direction: column;
      gap: 32px;
    }

    .about-pill {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--gold-glow);
      border: 1px solid rgba(201,169,110,0.15);
      border-radius: 2px;
      padding: 8px 16px;
      width: fit-content;
    }
    .about-pill-text {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--gold);
    }

    /* Waitlist */
    .waitlist-section {
      background: var(--black);
      text-align: center;
    }

    .waitlist-inner {
      max-width: 600px;
      margin: 0 auto;
    }

    .waitlist-form {
      display: flex;
      gap: 0;
      margin-top: 48px;
      border: 1px solid var(--border-bright);
      border-radius: 2px;
      overflow: hidden;
      transition: border-color 0.2s ease;
    }
    .waitlist-form:focus-within {
      border-color: rgba(201,169,110,0.4);
    }

    .waitlist-input {
      flex: 1;
      background: var(--surface);
      border: none;
      outline: none;
      padding: 18px 24px;
      font-family: 'Outfit', sans-serif;
      font-size: 15px;
      font-weight: 300;
      color: var(--white);
      letter-spacing: 0.01em;
    }
    .waitlist-input::placeholder {
      color: var(--white-dimmer);
    }

    .waitlist-btn {
      background: var(--gold);
      border: none;
      padding: 18px 28px;
      font-family: 'DM Mono', monospace;
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--black);
      cursor: none;
      white-space: nowrap;
      transition: background 0.2s ease;
    }
    .waitlist-btn:hover { background: #d4b47e; }

    .waitlist-note {
      font-family: 'DM Mono', monospace;
      font-size: 11px;
      font-weight: 300;
      letter-spacing: 0.08em;
      color: var(--white-dimmer);
      margin-top: 20px;
    }

    .waitlist-success {
      display: none;
      flex-direction: column;
      align-items: center;
      gap: 12px;
      padding: 32px;
      background: var(--green-dim);
      border: 1px solid rgba(74,222,128,0.2);
      border-radius: 2px;
      margin-top: 48px;
    }
    .waitlist-success.show { display: flex; }
    .waitlist-success-icon {
      font-size: 24px;
    }
    .waitlist-success-text {
      font-family: 'Outfit', sans-serif;
      font-size: 16px;
      font-weight: 300;
      color: var(--green);
    }

    /* Contact */
    .contact-section {
      background: var(--surface);
      border-top: 1px solid var(--border);
    }

    .contact-inner {
      display: flex;
      align-items: flex-end;
      justify-content: space-between;
      gap: 60px;
    }

    .contact-links {
      display: flex;
      flex-direction: column;
      gap: 16px;
      margin-top: 32px;
    }

    .contact-link {
      display: flex;
      align-items: center;
      gap: 16px;
      text-decoration: none;
      color: var(--white-dim);
      transition: color 0.2s ease;
      font-size: 15px;
      font-weight: 300;
    }
    .contact-link:hover { color: var(--white); }
    .contact-link-label {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 400;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--gold);
      width: 60px;
      flex-shrink: 0;
    }

    /* Footer */
    footer {
      background: var(--black);
      border-top: 1px solid var(--border);
      padding: 40px 48px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .footer-logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 15px;
      font-weight: 300;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--white-dimmer);
    }
    .footer-logo span { color: var(--gold); }

    .footer-copy {
      font-family: 'DM Mono', monospace;
      font-size: 10px;
      font-weight: 300;
      letter-spacing: 0.1em;
      color: var(--white-dimmer);
    }

    /* Animations */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(0.8); }
    }

    /* Scroll reveal */
    .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }
    .reveal-delay-4 { transition-delay: 0.4s; }

    /* Mobile */
    @media (max-width: 768px) {
      nav { padding: 20px 24px; }
      .nav-links { display: none; }
      section { padding: 80px 24px; }
      .hero { padding: 100px 24px 60px; }
      .hero-stat-row { display: none; }
      .kae-inner { grid-template-columns: 1fr; gap: 48px; }
      .flow-grid { grid-template-columns: 1fr 1fr; }
      .about-inner { grid-template-columns: 1fr; gap: 48px; }
      .contact-inner { flex-direction: column; }
      footer { flex-direction: column; gap: 16px; text-align: center; }
      body { cursor: auto; }
      .cursor, .cursor-ring { display: none; }
    }
  </style>
</head>
<body>

  <div class="cursor" id="cursor"></div>
  <div class="cursor-ring" id="cursorRing"></div>

  <!-- Nav -->
  <nav>
    <a href="/" class="nav-logo">Artisan <span>Affiliated</span></a>
    <ul class="nav-links">
      <li><a href="#kae">KAE</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="#waitlist" class="nav-cta">Early Access</a></li>
    </ul>
  </nav>

  <!-- Hero -->
  <section class="hero" id="home">
    <div class="hero-bg"></div>
    <div class="hero-grid"></div>

    <div class="hero-eyebrow">Artisan Affiliated — Est. 2025</div>

    <h1 class="hero-title">
      The future of<br/><em>food commerce</em><br/>is here.
    </h1>

    <p class="hero-sub">
      We build infrastructure for the commercial food supply chain — starting with KAE, the AI ordering assistant that gives independent restaurants their time back.
    </p>

    <div class="hero-actions">
      <a href="#waitlist" class="btn-primary">
        Get Early Access
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M1 7h12M7 1l6 6-6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
      <a href="#kae" class="btn-secondary">
        Learn about KAE
        <span class="arrow">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M1 7h12M7 1l6 6-6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </span>
      </a>
    </div>

    <div class="hero-stat-row">
      <div class="hero-stat">
        <div class="hero-stat-number">$100B</div>
        <div class="hero-stat-label">U.S. Food Supply Chain Market</div>
      </div>
      <div class="hero-stat">
        <div class="hero-stat-number">5+ hrs</div>
        <div class="hero-stat-label">Avg. Weekly Ordering Time Per Restaurant</div>
      </div>
      <div class="hero-stat">
        <div class="hero-stat-number">Zero</div>
        <div class="hero-stat-label">Unified Data Infrastructure Today</div>
      </div>
    </div>
  </section>

  <!-- KAE Section -->
  <section class="kae-section" id="kae">
    <div class="kae-inner">
      <div class="kae-left">
        <div class="section-label reveal">Product</div>
        <h2 class="section-title reveal reveal-delay-1">Meet <em>KAE.</em><br/>Your AI ordering<br/>assistant.</h2>
        <p class="section-body reveal reveal-delay-2" style="margin-top:24px;">
          KAE eliminates the most time-consuming part of running a restaurant: ordering. Tell KAE what you need in plain English. It handles the rest — routing orders to every vendor, sending professional order emails, and confirming everything back to you automatically.
        </p>
        <div class="kae-badge reveal reveal-delay-3" style="margin-top:40px;">
          <div class="kae-badge-dot"></div>
          <span class="kae-badge-text">Pilot launching Q2 2026 — Chester County, PA</span>
        </div>
      </div>

      <div class="kae-right reveal reveal-delay-2">
        <div class="feature-list">
          <div class="feature-item">
            <span class="feature-num">01</span>
            <div class="feature-content">
              <div class="feature-title">Natural Language Ordering</div>
              <div class="feature-desc">Type your order the way you think it. KAE understands context, quantities, and your regular items without rigid forms.</div>
            </div>
          </div>
          <div class="feature-item">
            <span class="feature-num">02</span>
            <div class="feature-content">
              <div class="feature-title">Multi-Vendor Intelligence</div>
              <div class="feature-desc">KAE knows which items come from which vendors. One input, automatically routed to every supplier simultaneously.</div>
            </div>
          </div>
          <div class="feature-item">
            <span class="feature-num">03</span>
            <div class="feature-content">
              <div class="feature-title">Automated Confirmation</div>
              <div class="feature-desc">Professional order emails sent instantly. You're notified the moment each vendor confirms — no follow-up calls needed.</div>
            </div>
          </div>
          <div class="feature-item">
            <span class="feature-num">04</span>
            <div class="feature-content">
              <div class="feature-title">Built on Your Workflow</div>
              <div class="feature-desc">KAE learns your vendors, your regular items, and your preferences during setup. No new systems for your vendors to adopt.</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section class="flow-section">
    <div class="section-label reveal">How It Works</div>
    <h2 class="section-title reveal reveal-delay-1">From order to<br/><em>confirmed</em> in minutes.</h2>

    <div class="flow-grid">
      <div class="flow-step reveal">
        <div class="flow-step-num">01</div>
        <div class="flow-step-title">Tell KAE what you need</div>
        <div class="flow-step-desc">Type your order naturally. "I need the usual produce plus extra tomatoes and chicken thighs from my protein vendor."</div>
        <div class="flow-step-accent"></div>
      </div>
      <div class="flow-step reveal reveal-delay-1">
        <div class="flow-step-num">02</div>
        <div class="flow-step-title">AI structures and routes</div>
        <div class="flow-step-desc">KAE parses your order, maps every item to the correct vendor, and asks for clarification on anything ambiguous.</div>
        <div class="flow-step-accent"></div>
      </div>
      <div class="flow-step reveal reveal-delay-2">
        <div class="flow-step-num">03</div>
        <div class="flow-step-title">Orders sent automatically</div>
        <div class="flow-step-desc">Professional order emails go out to each vendor simultaneously — with your restaurant name, line items, and delivery date.</div>
        <div class="flow-step-accent"></div>
      </div>
      <div class="flow-step reveal reveal-delay-3">
        <div class="flow-step-num">04</div>
        <div class="flow-step-title">You get confirmed</div>
        <div class="flow-step-desc">KAE notifies you the moment each vendor confirms. Your order dashboard stays current without any manual tracking.</div>
        <div class="flow-step-accent"></div>
      </div>
    </div>
  </section>

  <!-- About -->
  <section class="about-section" id="about">
    <div class="about-inner">
      <div class="reveal">
        <div class="section-label">About</div>
        <h2 class="section-title">Built by operators,<br/>for <em>operators.</em></h2>
        <blockquote class="about-quote">
          "The infrastructure the food industry has needed for 30 years. We're the team to build it."
        </blockquote>
      </div>

      <div class="about-details">
        <p class="section-body reveal">
          Artisan Affiliated is a food commerce technology company based in Chester County, Pennsylvania. We build software infrastructure for independent restaurants, local producers, and regional distributors — the businesses that define American food culture.
        </p>
        <p class="section-body reveal reveal-delay-1">
          KAE is our first product. It was born from years spent inside restaurant operations — watching talented operators waste hours every week on phone calls, fragmented texts, and manual order tracking that could and should be automated.
        </p>
        <p class="section-body reveal reveal-delay-2">
          We're moving fast. Piloting locally. And building the data foundation that will power the next generation of food commerce tools.
        </p>
        <div class="reveal reveal-delay-3">
          <div class="about-pill">
            <span class="about-pill-text">Chester County, PA — Est. 2025</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Waitlist -->
  <section class="waitlist-section" id="waitlist">
    <div class="waitlist-inner">
      <div class="section-label reveal" style="justify-content:center;">Early Access</div>
      <h2 class="section-title reveal reveal-delay-1">Be first on <em>KAE.</em></h2>
      <p class="section-body reveal reveal-delay-2" style="margin: 0 auto; text-align:center;">
        We're onboarding our first cohort of pilot restaurants in Chester County. Join the waitlist and we'll reach out directly.
      </p>

      <div class="waitlist-form reveal reveal-delay-3" id="waitlistForm">
        <input
          type="email"
          class="waitlist-input"
          id="emailInput"
          placeholder="your@restaurant.com"
          autocomplete="email"
        />
        <button class="waitlist-btn" onclick="handleWaitlist()">
          Join Waitlist
        </button>
      </div>

      <div class="waitlist-success" id="waitlistSuccess">
        <div class="waitlist-success-icon">✓</div>
        <div class="waitlist-success-text">You're on the list. We'll be in touch shortly.</div>
      </div>

      <p class="waitlist-note reveal">No spam. No pressure. Just a direct conversation about your restaurant.</p>
    </div>
  </section>

  <!-- Contact -->
  <section class="contact-section" id="contact">
    <div class="contact-inner">
      <div>
        <div class="section-label reveal">Contact</div>
        <h2 class="section-title reveal reveal-delay-1">Let's talk<br/><em>food commerce.</em></h2>
        <p class="section-body reveal reveal-delay-2" style="margin-top:20px;">
          Whether you're a restaurant operator, distributor, investor, or just curious — we want to hear from you.
        </p>
      </div>

      <div class="contact-links reveal reveal-delay-2">
        <a href="mailto:artisanaffiliated&#64;gmail.com" class="contact-link">
          <span class="contact-link-label">Email</span>
          artisanaffiliated&#64;gmail.com
        </a>
        <a href="mailto:elisha&#64;artisanaffiliated.com" class="contact-link">
          <span class="contact-link-label">Founder</span>
          elisha&#64;artisanaffiliated.com
        </a>
        <a href="#waitlist" class="contact-link">
          <span class="contact-link-label">KAE</span>
          Join the pilot waitlist →
        </a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="footer-logo">Artisan <span>Affiliated</span></div>
    <div class="footer-copy">© 2025 Artisan Affiliated LLC — Chester County, PA</div>
  </footer>

  <script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>
    // Custom cursor
    const cursor = document.getElementById('cursor');
    const ring = document.getElementById('cursorRing');
    let mouseX = 0, mouseY = 0, ringX = 0, ringY = 0;

    document.addEventListener('mousemove', e => {
      mouseX = e.clientX;
      mouseY = e.clientY;
      cursor.style.left = mouseX + 'px';
      cursor.style.top = mouseY + 'px';
    });

    function animateRing() {
      ringX += (mouseX - ringX) * 0.12;
      ringY += (mouseY - ringY) * 0.12;
      ring.style.left = ringX + 'px';
      ring.style.top = ringY + 'px';
      requestAnimationFrame(animateRing);
    }
    animateRing();

    document.querySelectorAll('a, button, input').forEach(el => {
      el.addEventListener('mouseenter', () => {
        cursor.style.width = '16px';
        cursor.style.height = '16px';
        ring.style.width = '56px';
        ring.style.height = '56px';
        ring.style.opacity = '0.5';
      });
      el.addEventListener('mouseleave', () => {
        cursor.style.width = '8px';
        cursor.style.height = '8px';
        ring.style.width = '36px';
        ring.style.height = '36px';
        ring.style.opacity = '1';
      });
    });

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver(entries => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
        }
      });
    }, { threshold: 0.12 });
    reveals.forEach(el => observer.observe(el));

    // Waitlist
    function handleWaitlist() {
      const email = document.getElementById('emailInput').value;
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

      if (!emailRegex.test(email)) {
        document.getElementById('emailInput').style.borderColor = 'rgba(255,100,100,0.4)';
        document.getElementById('emailInput').placeholder = 'Please enter a valid email';
        setTimeout(() => {
          document.getElementById('emailInput').style.borderColor = '';
          document.getElementById('emailInput').placeholder = 'your@restaurant.com';
        }, 2000);
        return;
      }

      // Store locally and show success
      const entries = JSON.parse(localStorage.getItem('kae_waitlist') || '[]');
      entries.push({ email, timestamp: new
